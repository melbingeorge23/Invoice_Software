# spring.application.name=invoice-software-backend

# spring.datasource.url=jdbc:mysql://localhost:3306/invoice_db
# spring.datasource.username=root
# spring.datasource.password=Melbin@123
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true

# server.port=8080


spring.application.name=invoiceapp

server.port=${PORT:8080}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/invoice_db}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Melbin@123}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

jwt.secret=${JWT_SECRET:invoice_software_secret_key_for_jwt_token_2026_secure_key}

app.frontend-url=${FRONTEND_URL:http://localhost:5173}